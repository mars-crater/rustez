[package]
name = "rustez-agent"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
rustez-usage = { path = "../rustez-usage" }

[lints]
workspace = true
